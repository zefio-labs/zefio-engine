flows:
  - name: inbound
    label: "4_TCP_Massive_Relay"
    options:
      threadPool:
        corePoolSize: 200
        maxPoolSize: 400
        queueCapacity: 0
        autoScaling:
          enabled: true
          threshold: 0.7
          checkInterval: 5
          scaleUpStep: 30
          scaleDownStep: 10
      cpuQueue:
        capacity: 50000
      ioQueue:
        capacity: 20000

    # 🎯 Architecture Left: Async Plain (TCP + Fixed Message + Async Profile)
    ingress:
      name: from
      label: "Source Inbound (51004)"
      type: TcpIngress
      telegram: fixed-standard
      profile: ingress-async
      exchangePattern: RequestReply
      config:
        port: 51004
        requestEncoding: utf-8
        responseEncoding: utf-8

    # 🎯 Architecture Right: Sync Plain (TCP + Fixed Message + Sync Massive Profile)
    steps:
      - name: to
        label: "Target Request (TCP_Massive)"
        type: TcpUpstream
        telegram: fixed-standard
        profile: upstream-sync-massive
        exchangePattern: RequestReply
        retry:
          enabled: false
        config:
          host: 192.168.3.218
          port: 51104
          requestEncoding: utf-8
          responseEncoding: utf-8
    on-error:
      - error-type: ANY
        refErrorHandler: fixederror
